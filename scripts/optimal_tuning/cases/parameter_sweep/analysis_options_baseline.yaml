general:
  folder_output: ../../../../data/optimal_tuning/parameter_sweep/
  fname_output: ps_vs_ipc_baseline

design_variables:
  control:
    ps_percent:
      flag: true
      lower_bound: 0.5
      upper_bound: 1.0

merit_figure_user: 
  name: aeroelastic.AEP
  ref: 81000000
  max_flag: true

driver:
  design_of_experiments:
    flag: true
    run_parallel: true
    generator: FullFact
    num_samples: 6  # For FullFact, this is not the amount of samples, but amount of levels.

recorder:
  flag: true
  file_name: ps_vs_ipc_baseline.sql
  includes: ["aeroelastic.*", "tune_rosco_ivc.*"]  # Just "*" doesn't work due to some non-uniqueness problem.
